<h1>Reuniões</h1>
<%= link_to 'Actas', records_path %>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @meetings.each do |meeting| %>
    <th>
      <tr>
        <td>Assunto</td>
        <td>Ordem de trablhos</td>
        <td>Data</td>
        <td>Departamento</td>
      </tr>
    </th>
    <tbody>
      <tr>
        <td> <%= meeting.assunto %> </td>
        <td> <%= meeting.desc %> </td>
        <td> <%= meeting.data %> </td>
        <td> <%= meeting.dep.try(:name) %> </td>
        <td><%= link_to 'Show', meeting %></td>
        <td><%= link_to 'Edit', edit_meeting_path(meeting) %></td>
        <td><%= link_to 'Destroy', meeting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    </tbody>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Voltar', home_index_path %>
<%= link_to 'Nova Reuniao', new_meeting_path %>




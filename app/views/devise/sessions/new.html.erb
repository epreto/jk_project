<div class="col-login">
  <div class="logo"></div>
  <div class="login">

    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
        <div class = "mail"> <%= f.email_field :email, :autofocus => true, placeholder: "Insira o E-mail", class: "userlogo" %> </div> <div class="userblue"></div>
        <div class = "pass"> <%= f.password_field :password, placeholder: "Insira a Palavra-passe", class: "passlogo" %> </div> <div class="passblue"></div>

      </div>

        <% if devise_mapping.rememberable? %>
        <% end %>
        <div class="login">
        <div class = "iniciar"><%= f.submit "Iniciar Sessão" %></div>
        <div class = "recuperar"><p><%= link_to "Recuperar Password", new_password_path(resource_name) %></p></div>
    <% end %>
  </div>
</div>


<!-- <%= render "devise/shared/links" %> -->